# المساهمة في أثر

شكرًا لاهتمامك بإثراء «أثر». نرحب بالمقالات والتحسينات التي تزيد الدقة، الوضوح، وإمكانية الوصول.

## قبل البدء

1. افتح Issue للمشكلة أو الاقتراح الكبير قبل العمل عليه.
2. أنشئ فرعًا صغيرًا باسم وصفي، مثل `content/add-article-about-andalus`.
3. لا تضف معلومات تاريخية بلا مصادر قابلة للتحقق.

## تطوير محلي

```bash
npm install
npm run dev
npm test
npm run build
```

لإنشاء مقال، شغّل `npm run article:editor`. راجع المعاينة، احفظ المقال، ثم افحص الملف الناتج والفهرس قبل الإرسال.

## إرشادات المقالات

- اكتب بالعربية الواضحة، مع الحفاظ على المصطلحات والأسماء التاريخية بدقة.
- اجعل لكل مقال `slug` فريدًا بأحرف إنجليزية صغيرة وشرطات فقط.
- استخدم أدوات العناوين الدلالية (`h2` ثم `h3`) في المحرر كي يُبنى جدول المحتويات تلقائيًا؛ لا حاجة لكتابة HTML.
- أضف ملخصًا موجزًا، ووسومًا مفيدة، ومصادرًا بروابط مباشرة.
- أضف `alt` وصفيًا للصور، ولا تعتمد على اللون وحده في نقل المعنى.
- تجنب إدخال JavaScript أو HTML غير ضروري داخل محتوى المقال.

## Pull Requests

- اجعل كل Pull Request مركزًا على هدف واحد.
- اشرح ما تغيّر وكيف تم التحقق منه.
- شغّل `npm test` و`npm run build` قبل الإرسال.
- حدّث التوثيق إذا تغيّر أمر تشغيل أو بنية ملف.
- لا تضع أسرارًا أو بيانات شخصية أو ملفات build في المستودع.

## أسلوب المراجعة

نراجع الدقة، سلامة المصادر، سهولة القراءة، إمكانية الوصول، وحجم الحزمة. الملاحظات موجهة للكود أو المحتوى، وتحافظ على الاحترام وحسن النية.
